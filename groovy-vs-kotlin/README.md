Shows differences between the same Gradle project implemented in the Groovy and Kotlin DSLs.

## Running

Run via *application* plugin:

`./gradlew run`

Run jar file directly:

`./gradlew runJar`

## Testing

`./gradlew test`